// Represents a simple cylindrical battery cell model for 3D rendering or printing
module battery() {
    // Create a cylinder with radius 7mm and height 50mm to approximate a standard AA battery
    cylinder(r = 7, h = 50);
}